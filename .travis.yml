## Need sudo to add ppas
sudo: required

language: cpp
compiler: gcc
branches:
  only:
    - unstable

before_install:
  ## For gcc 4.8
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  ## For OpenCV
  - sudo add-apt-repository ppa:yjwong/opencv2 -y
  - sudo apt-get update -qq
  - if [ "$CXX" = "g++" ]; then sudo apt-get install -qq g++-4.8; fi
  - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi
  ## Use Miniconda for Python.
  - wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
  - chmod +x miniconda.sh
  - ./miniconda.sh -b
  - export PATH=/home/travis/miniconda/bin:$PATH

install:
  - sudo apt-get install -qq m4 doxygen libboost-all-dev libeigen3-dev libopencv-dev
  - conda update --yes conda
  - travis_retry conda install --yes pip numpy scipy scikit-image scikit-learn jinja2 matplotlib pillow scons
  - sudo pip install networkx
  - sudo pip install cppheaderparser

before_script:
  - git submodule update --init --recursive
  - export EIGEN_ROOT=/usr/local/include/eigen3

script:
  - scons --generate-interfaces
  - scons --with-serialization --with-tests --with-examples --with-python

after_success: true

after_failure:
  - cat config.log

after_script: true

